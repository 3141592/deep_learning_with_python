#!/usr/bin/env bash
set -euo pipefail

REPO_ROOT="$(cd "$(dirname "$0")" && pwd)"
export PYTHONPATH="${REPO_ROOT}${PYTHONPATH:+:${PYTHONPATH}}"

TARGET="${1:-}"
if [[ -z "$TARGET" ]]; then
  echo "Usage: $0 path/to/script.py [args...]"
  exit 1
fi

exec /home/roy/venvs/torch/bin/python "$@"